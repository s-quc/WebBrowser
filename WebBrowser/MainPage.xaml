<Page
    x:Class="WebBrowser.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="233*"/>
            <ColumnDefinition Width="517*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="btnHome" Content="Home" Margin="30,45,0,0" VerticalAlignment="Top" Click="Button_Click" Width="71"/>
        <WebView x:Name="wvMain" Margin="0,123,134,0" LoadCompleted="WebView_LoadCompleted" Grid.ColumnSpan="2"/>
        <Button x:Name="bBack" Content="Back" Margin="0,45,0,0" VerticalAlignment="Top" Width="71" HorizontalAlignment="Center" Click="bBack_Click"/>
        <Button x:Name="bForward" Content="Forward" Margin="316,45,0,0" VerticalAlignment="Top" Click="bForward_Click"/>

    </Grid>
</Page>
